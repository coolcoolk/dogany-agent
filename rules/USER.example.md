# USER

Nothing recorded yet. The main agent fills this over time with STABLE PROFILE
FACTS ONLY (name, how addressed, timezone, job, relationships, domain core
constants) -- one fact per line with (date, source). Procedures / output
formats / session mechanics never go here (AGENT.md or SKILL.md); unconfirmed
preferences go to engine memories. This is the committed seed; the real
USER.md is gitignored.
