# hot.custom.md -- this instance's operating-gate overlay (instance-owned)

Instance-owned (`custom` name token = updates NEVER touch this file). Seeded
empty on purpose: with no section below, every gate in `rules/hot.framework.md`
applies exactly as written. Add a section here ONLY to diverge from a
same-titled framework gate -- the hub loads this file after the canon, so the
overlay section wins. Agent proposes the edit, user confirms.
