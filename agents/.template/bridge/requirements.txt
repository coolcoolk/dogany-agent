# Core deps -- required to import and run the bridge. mint.sh --core-only
# installs every line ABOVE the optional marker (single source, DGN-674 F2):
# add new CORE deps above the marker, optional/heavy extras below it.
# >=20.8 for LinkPreviewOptions (DGN-376 link-preview suppression)
python-telegram-bot>=20.8
claude-agent-sdk>=0.1.72
pydantic>=2
pydantic-settings>=2
python-dotenv
# --- optional: voice input (heavy; installed only when voice is enabled) ---
faster-whisper
# --- dev/test only (never installed by mint.sh --core-only) ---
# pytest-asyncio: async tests (turn_death, DGN-616 coalescing) use
# @pytest.mark.asyncio and fail to run without the plugin.
pytest
pytest-asyncio
